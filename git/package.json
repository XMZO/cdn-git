{
  "name": "cdn-git-proxy",
  "version": "0.1.0",
  "description": "Proxy raw.githubusercontent.com with optional GitHub token auth",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  },
  "engines": {
    "node": ">=18"
  }
}

