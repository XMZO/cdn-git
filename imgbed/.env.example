# 默认代理目标 (Hugging Face Space 或其他后端)
DEFAULT_TARGET=xxxxxxxxxxxxxxxxx.hf.space

# 域名映射 (JSON 格式，可选，用于多域名映射到不同后端)
# 示例: {"a.com":xxxxxxxxxxxxxxxxx.pages.dev","img.example.com":"another-backend.pages.dev"}
HOST_MAPPING={}

# Worker 验证密钥 (与后端配置一致，用于验证请求来源)
WORKER_SECRET_KEY=your_secret_key_here

# 服务端口
PORT=3000
