# NOTE: 这是旧的独立实现（维护模式）。当前部署请使用仓库根目录 README 指引（`go/` 目录的部署文件）。
services:
  proxy:
    build: .
    container_name: torcherino
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env

