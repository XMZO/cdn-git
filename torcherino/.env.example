# 默认代理目标 (Hugging Face Space 或其他后端)
# 格式: {用户名}-{space名称}.hf.space 或 {项目名}.pages.dev
DEFAULT_TARGET=your-username-space.hf.space

# 域名映射 (JSON 格式，可选，用于多域名映射到不同后端)
# 示例: {"example.com":"your-project.pages.dev","img.example.com":"another.hf.space"}
HOST_MAPPING={}

# Worker 验证密钥 (与后端配置一致，用于验证请求来源)
WORKER_SECRET_KEY=your_secret_key_here

# 可选：自定义转发给上游的验证 Header 名称（逗号分隔）。
# 为空时默认使用 x-forwarded-by-worker。
WORKER_SECRET_HEADERS=

# 可选：不同 Header 使用不同值（JSON 对象）；会覆盖同名 Header 的 WORKER_SECRET_KEY 设置。
# 示例：{"x-forwarded-by-worker":"secretA","x-another-secret":"secretB"}
WORKER_SECRET_HEADER_MAP={}

# 服务端口
PORT=3000
