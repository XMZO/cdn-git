# NOTE: 这是旧的独立实现（维护模式）的 `.env` 示例；当前推荐配置请使用 `go/.env.example`。

# CDN 源地址
ASSET_URL=https://cdn.jsdelivr.net

# 允许的 GitHub 用户列表 (逗号分隔)
ALLOWED_GH_USERS=XMZO,starsei

# 默认 GitHub 用户 (当请求路径不包含用户名时使用)
DEFAULT_GH_USER=XMZO

# Redis 配置
REDIS_HOST=redis
REDIS_PORT=6379

# 服务端口
PORT=3000
